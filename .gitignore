# Dependencias
node_modules/

# Segredos (chaves de API, etc) — sempre ficar fora do git
config.local.json
.env
.env.local

# Arquivos de ferramentas de IA (especificos da sua maquina)
.claude/
.claudiomiro/

# Estado runtime (banco SQLite + WAL)
dedup.db
dedup.db-wal
dedup.db-shm

# Cache de CSVs do IBAMA (~160 MB, regenerado pelo proprio codigo)
ibama-cache/

# Logs do dia-a-dia
logs/

# Saidas das execucoes diarias
relatorio-*.json

# Artefatos antigos de debug do scraper DOU
dou-debug.png
dou-raw.html
dou-rendered.html
dou-network.json
dou-resultados.json
